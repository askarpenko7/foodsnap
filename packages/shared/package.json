{
  "name": "@foodsnap/shared",
  "version": "0.0.0",
  "private": true,
  "description": "API contract shared by the mobile app, the gateway and the nutrition service",
  "license": "MIT",
  "author": "Alexander Karpenko (https://github.com/askarpenko7)",
  "//": "Source-only package: no build step. Metro compiles TS directly and tsup bundles workspace sources, so shipping src keeps one copy of the contract with no dist to fall out of date.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest"
  },
  "devDependencies": {
    "@jest/globals": "29.7.0",
    "@types/jest": "29.5.14",
    "jest": "29.7.0",
    "ts-jest": "29.4.12",
    "typescript": "5.9.3"
  }
}
